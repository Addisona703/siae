1.sql
```text
请根据提供的数据库表结构SQL和相关文档，自动生成对应的Java实体类，要求如下：

1. 实体类必须放在 `entity` 包中。
2. 代码规范，符合Java命名规范和企业级开发标准。
3. 类名使用表名的驼峰式命名（去掉下划线，首字母大写）。
4. 字段名使用驼峰式命名，与数据库字段对应。
5. 添加必要的注解，例如 `@Entity`、`@Table`（如果使用JPA）、`@Column` 等。
6. 生成标准的getter/setter方法。
7. 支持序列化，实现 `Serializable` 接口。
8. 添加适当的JavaDoc注释，简要说明类和字段含义。
9. 日期类型、数字类型等要映射到合适的Java数据类型。
10. 根据文档补充字段的注释。

请根据以上要求，结合SQL建表语句和文档描述，生成完整的Java实体类代码。

```


